# Build Log

- Ensured YouTube API requests use the provided channel credentials without local filtering or mock fallbacks.
- Navigation bar background now always includes a blur with consistent styling across scroll states.
- Added note to rely on real YouTube content pulled via `VITE_YOUTUBE_API_KEY` and `VITE_YOUTUBE_CHANNEL_ID` during builds.
- Removed the forum experience and associated Supabase forum schema and navigation links.
